const User=require("../models/User");module.exports={async index(req,res){const users=await User.findAll();return res.json(users)},async store(req,res){const{name:name,email:email}=req.body,user=await User.create({name:name,email:email});return res.json(user)}};