const Sequelize=require("sequelize"),dbConfig=require("../database/config/database"),User=require("../models/User"),Address=require("../models/Address"),connection=new Sequelize(dbConfig);User.init(connection),Address.init(connection),User.associate(connection.models),Address.associate(connection.models),module.exports=connection;